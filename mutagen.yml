sync:
  defaults:
    flushOnCreate: true
    ignore:
      paths:
        - "build/"
        - ".cache/"
        - "checkpoints"
        - ".git/"
  aurora-sync:
    alpha: "~/Documents/AURORA"
    beta: "rome:~/scratch/aurora_project"
